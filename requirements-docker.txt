# requirements-docker.txt — full dependency set for the Docker image.
# Includes server, MCP, Spark, and all backends.
# For the PyPI SDK package, see requirements.txt (core only).

# -------- Core data stack --------
numpy>=1.26,<3.0
pandas>=2.1,<2.3
pyarrow>=16,<19
pytz>=2023.3
tzdata>=2023.3

# -------- Engine / query / utils --------
duckdb>=1.1,<2.0
polars>=1.6,<2.0
sqlglot>=26,<27
colorlog>=6.9,<7.0
python-dotenv>=1.0,<2.0
setuptools>=68

# -------- Catalog / locking --------
redis>=5.0,<6.0

# -------- Storage backends --------
minio>=7.2,<8.0
boto3>=1.34,<2.0

# -------- Server (API, UI, MCP) --------
fastapi>=0.111,<0.117
uvicorn[standard]>=0.30,<0.34
jinja2>=3.1,<4.0
aiofiles>=23.2,<24.0
orjson>=3.9,<4.0
httpx>=0.27,<1.0
anyio>=4.4,<5.0
mcp>=1.1.0

# -------- WebSocket (notebook + spark_plug) --------
websockets>=12.0,<14.0

# -------- Spark Thrift --------
pyhive>=0.7,<1.0
thrift>=0.16,<1.0
pure-sasl>=0.6,<1.0
thrift-sasl>=0.4,<1.0
