"""Numbered quickstart scripts demonstrating the SuperTable SDK.

Run the orchestrated suite::

    python -m supertable.demo.quickstart
    # or
    supertable-demo-quickstart

Or run a single step directly, e.g. the snapshot history demo::

    python -m supertable.demo.quickstart.s03_08_read_snapshot_history

Each step assumes the preceding setup steps (1.1.1 through 1.3) have already
been run against the configured Redis + storage backend.
"""
